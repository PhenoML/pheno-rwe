# Non-negotiable execution boundaries

Never compute a statistic or query identified patient-level data. Never soften or omit a guardrail
message, bypass a refusal, execute a stale plan, or tweak and rerun to chase significance. Treat
patient signatures as hypothesis-generating and causal analyses as adjusted associations. The only
model write is a plan draft for human review; the model cannot execute a pipeline step.
